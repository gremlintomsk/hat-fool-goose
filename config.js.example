// Публичный OAuth Client ID из Google Cloud Console (не секрет).
// Создать: https://console.cloud.google.com/apis/credentials → OAuth client ID → Web application,
window.GAME_CONFIG = {
  googleClientId: 'your-token.apps.googleusercontent.com',
};
