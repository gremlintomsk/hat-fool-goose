# Линт api.py: корректность и стиль, без придирок к осознанным решениям
# (except Exception при проверке токена — намеренный, см. комментарий в коде).
line-length = 100

[lint]
select = ["E", "F", "W", "UP", "SIM"]
